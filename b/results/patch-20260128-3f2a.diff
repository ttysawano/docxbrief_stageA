--- src/docxbrief/cli.py
+++ src/docxbrief/cli.py
@@ -9,7 +9,7 @@
 from .build import build_summary, update_summary
 from .shell import run_shell
 from .status import show_status
-from .reset import reset_project
+from .reset import reset_project, clear_changelog
 
 
 def _add_common_args(p: argparse.ArgumentParser) -> None:
@@ -49,6 +49,10 @@
     p_reset.add_argument("--all", action="store_true", help="Also remove output summary.adoc")
     p_reset.add_argument("--yes", action="store_true", help="Skip confirmation")
 
+    p_changelog = sub.add_parser("changelog-clear", help="Clear changelog entries in manifest.")
+    _add_common_args(p_changelog)
+    p_changelog.add_argument("--yes", action="store_true", help="Skip confirmation")
+
     args = parser.parse_args(argv)
 
     if args.cmd == "init":
@@ -82,6 +86,15 @@
         show_status(cfg)
         return 0
 
+
+    if args.cmd == "changelog-clear":
+        if not args.yes:
+            print("This will clear manifest changelog entries.")
+            print("Re-run with --yes to proceed.")
+            return 2
+        clear_changelog(cfg)
+        print("Changelog cleared.")
+        return 0
     if args.cmd == "reset":
         if not args.yes:
             print("This will delete .docxbrief/ (state)" + (" and summary.adoc" if args.all else "") + ".")
--- src/docxbrief/reset.py
+++ src/docxbrief/reset.py
@@ -3,6 +3,7 @@
 import shutil
 
 from .config import Config
+from .state import load_manifest, save_manifest
 
 
 def reset_project(cfg: Config, *, remove_summary: bool = False, remove_state: bool = True) -> None:
@@ -20,3 +21,10 @@
         out = cfg.output_adoc
         if out.exists():
             out.unlink()
+
+
+def clear_changelog(cfg: Config) -> None:
+    """Clear changelog entries in manifest.json."""
+    manifest = load_manifest(cfg)
+    manifest["changelog"] = []
+    save_manifest(cfg, manifest)
